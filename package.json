{
  "name": "jshook_plugin_android_archive_index",
  "private": false,
  "version": "0.0.1",
  "description": "jshook plugin for Android APK archive indexing and file triage.",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@jshookmcp/extension-sdk": "^0.3.2",
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "typescript": "^6.0.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
